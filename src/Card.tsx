const Card = ({ title, img, text }) => {
  return (
    <div>
      <h1>Bananas</h1>
      <div>
        <h4>{title}</h4>
        <img src="" alt="" />
        <p>{text}</p>
      </div>
    </div>
  );
};

export default Card;
